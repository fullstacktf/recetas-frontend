import { FC} from 'react';
import styled from '@emotion/styled';

export interface PostTimelineProps{

}

export const PostTimeline: FC<PostTimelineProps> = (props) => {

  return <p>PostTimeline</p>;

};
